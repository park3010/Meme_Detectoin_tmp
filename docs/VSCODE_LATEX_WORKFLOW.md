# VS Code and LaTeX Workflow

The workspace recommends Python, Pylance, YAML, and LaTeX Workshop. LaTeX Workshop uses `latexmk`, `latex/build`, SyncTeX, build-on-save, and the editor-tab PDF viewer. Tasks cover research plan/preflight/smoke/aggregation/dashboard and paper export/main/supplement/draft/final checks.

Python tasks use `conda run -n meme_cikm`. No Overleaf credentials, remote Git action, API key, or automatic push is configured.
