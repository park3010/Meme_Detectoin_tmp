% !TeX root = ../main.tex
\section{Ethical Considerations}
Structured FHM evaluation uses agent-silver annotations rather than human gold labels. Human-evaluation exports remain unrated until independent annotators complete the supplied forms. The repository blocks unsupported external models and API methods instead of substituting unverified implementations.
