% !TeX root = ../main.tex
\section{Experimental Setup}
All model and threshold selection is restricted to the fixed HarMeme validation split. FHM labels are read only for final held-out evaluation. Run manifests record source hashes, configuration hashes, asset provenance, and audit status.

\begin{table}[t]
\caption{Protocol data summary. Counts are generated from immutable manifests.}
\label{tab:data}
\centering
\input{tables/generated/supp_per_dataset}
\end{table}
