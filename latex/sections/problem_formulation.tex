% !TeX root = ../main.tex
\section{Problem Formulation}
The locked evaluation separates HarMeme source domains from the FHM held-out target domain. Structured fields are evaluated only where label masks mark agent-silver annotations as eligible.
